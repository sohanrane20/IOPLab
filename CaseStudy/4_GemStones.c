/* Q. 4. John has discovered various rocks. Each rock is composed of various elements, and each element is represented by a lowercase latin letter from 'a' to 'z'. An element can be present multiple times in a rock. An element is called a 'gem-element' if it occurs at least once in each of the rocks. Program to display the number of gem-elements that exist in the given list of N rocks with their compositions.Author : Sohan*/#include <stdio.h>#include <stdlib.h>#include <string.h>int main(){	int N, i, j, k, flag, element, gem_element = 0;	printf("Enter number of rocks : ");	scanf("%d", &N);	if (N < 1 || N > 100)	{		printf("Invalid number of rocks\n");		exit(1);	}	char c[N][100];	for (i = 0; i < N; i++)	{		printf("Enter composition of rock : ");		scanf("%s", &c[i]);		for (j = 0; j < strlen(c[i]); j++)		{			if (c[i][j] < 'a' || c[i][j] > 'z')			{				printf("Composition can consist of lowercase letters of English alphabet only\n");				exit(1);			}		}	}	for (k = 0; k < strlen(c[0]); k++)	{		element = c[0][k];		for (i = 0; i < N; i++)		{			flag = 0;			for (j = 0; j < strlen(c[i]) && flag == 0; j++)			{				if (c[i][j] == element)				{					flag = 1;				}			}			if (flag == 0)			{				break;			}		}		if (flag == 1)		{			gem_element = gem_element + 1;		}	}	printf("Number of gem elements = %d", gem_element);	return 0;}